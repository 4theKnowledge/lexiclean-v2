import { Box, Typography } from "@mui/material";
import React from "react";

const Schema = () => {
  return (
    <div>
      <Box>
        <Typography>Update Tagging Scheme</Typography>
      </Box>
    </div>
  );
};

export default Schema;
